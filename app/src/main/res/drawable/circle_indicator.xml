<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <size
        android:width="12dp"
        android:height="12dp" />
    <solid android:color="@color/indicator_inactive_color" />

    <stroke
        android:width="1dp"
        android:color="@color/indicator_stroke_color" />
    <solid android:color="@color/indicator_stroke_color" />
</shape>